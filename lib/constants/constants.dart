const homePageTitle = 'TodoList';
const addTaskPageTitle = 'AddTask';
const editTaskPageTitle = 'EditTask';

const sliderMenuTitleForDeletion = 'Delete';
const sliderMenuTitleForEditing = 'Edit';

const snackBarMessageForAdditions = 'task add success!';
const snackBarMessageForEditing = 'task edit success!';
const snackBarMessageForDeletion = 'task delete success!';

const placeholdersForInputFields = 'Input task';

const addButtonTitle = 'Add';
const editButtonTitle = 'Edit';

const demoTaskMessage1 =
    'Tapping a task toggles between completed and uncompleted tasks.';
const demoTaskMessage2 = 'The task will look like this when it is completed.';
const demoTaskMessage3 = 'New tasks can be added via the plus button.';
const demoTaskMessage4 =
    'Edit and delete tasks by sliding from the right edge.';
